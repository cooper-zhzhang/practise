@Test
@Transactional
public void test011do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test012do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test013do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test014do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test015do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test016do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test017do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test018do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test019do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test020do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test021do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test022do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test023do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test024do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test025do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test026do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test027do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test028do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test029do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test030do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test031do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test032do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test033do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test034do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test035do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test036do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test037do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test038do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test039do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test040do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test041do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test042do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test043do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test044do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test045do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test046do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test047do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test048do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test049do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test050do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test051do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test052do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test053do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test054do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test055do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test056do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test057do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test058do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test059do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test060do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test061do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test062do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test063do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test064do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test065do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test066do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test067do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test068do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test069do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test070do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test071do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test072do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test073do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test074do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test075do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test076do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test077do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test078do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test079do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test080do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test081do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test082do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test083do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test084do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test085do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test086do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test087do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test088do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test089do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test090do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test091do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test092do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test093do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test094do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test095do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test096do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test097do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test098do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test099do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test100do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test101do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test102do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test103do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test104do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test105do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test106do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test107do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test108do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test109do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test110do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test111do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test112do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test113do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test114do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test115do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test116do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test117do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test118do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test119do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test120do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test121do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test122do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test123do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test124do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test125do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test126do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test127do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test128do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test129do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test130do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test131do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test132do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test133do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test134do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test135do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test136do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test137do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test138do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test139do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test140do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test141do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test142do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test143do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test144do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test145do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test146do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test147do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test148do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test149do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test150do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test151do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test152do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test153do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test154do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test155do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test156do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test157do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test158do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test159do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test160do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test161do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test162do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test163do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test164do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test165do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test166do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test167do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test168do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test169do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test170do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test171do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test172do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test173do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test174do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test175do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test176do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test177do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test178do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test179do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test180do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test181do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test182do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test183do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test184do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test185do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test186do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test187do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test188do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test189do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test190do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test191do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test192do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test193do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test194do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test195do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test196do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test197do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test198do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
@Test
@Transactional
public void test199do_Pay() {
  Order order = createOrder(OrderSource.XR_STORE;
  PaymentCreateForm payment1 = createPaymentForm(WX, order.getRealFee()/2)
  PaymentCreateForm payment2 = createPaymentForm(CUPON, order.getRealFee()-oeder.getRealFee/2)
  PaymentsCreateForm from = new PaymentsCreateForm();
  form.setOrderNo(order.getOrderNo());
  form.getPayments().add(payment1);
  form.getPayments().add(payment2);
  payService.doPay(form)
  checkPay(order.getId(), order.getRealFee()-order.getRealFee()/2)
}
